## What changed

<!-- One or two sentences. What does this PR do? -->

## Why

<!-- Motivation — bug fix, content update, new feature, SEO improvement, etc. -->

## SEO impact

<!-- Does this change affect metadata, structured data, page content, sitemap, or heading hierarchy? -->
- [ ] No SEO impact
- [ ] Updated meta title / description
- [ ] Added or changed structured data (JSON-LD)
- [ ] Changed visible page copy or headings
- [ ] Added or removed a page (sitemap affected)

## Checklist

- [ ] `npm run check` passes (Biome)
- [ ] `npm run build-types` passes (TypeScript)
- [ ] `npm run build` succeeds locally
- [ ] All `<img>` / `<Image>` have descriptive `alt` text
- [ ] No `<a>` nested inside `<Link>`
- [ ] New pages use `<Meta>` with `title`, `description`, and `jsonLd`
- [ ] No hardcoded contact info or URLs — uses `AppConfig`

## Screenshots / preview

<!-- For UI changes, attach a before/after screenshot or a Vercel preview link. -->
